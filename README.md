# ansible-swarm

Deploy Docker Swarm with Ansible. It is capable of scaling, just needs to provide more hosts to the inventory file and it will deploy docker and add to the swarm.

## Tested 
- CentOS 7 x86_64
- Ubuntu 18 armv7l (Armbian Bionic)